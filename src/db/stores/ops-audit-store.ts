import type { Queryable } from "./queryable.js";
export class OpsAuditStore { public constructor(private readonly db:Queryable){} public async record(input:{actor:string;action:string;jobName?:string;commandId?:string;details?:Record<string,unknown>}){await this.db.query("insert into ace_hunter.ops_audit_log(actor,action,job_name,command_id,details) values($1,$2,$3,$4,$5::jsonb)",[input.actor,input.action,input.jobName??null,input.commandId??null,JSON.stringify(input.details??{})]);} public async list(limit=100){const r=await this.db.query("select * from ace_hunter.ops_audit_log order by created_at desc limit $1",[limit]); return r.rows;} }
