/* eslint-disable @typescript-eslint/no-explicit-any */
import { describe, expect, it, vi } from "vitest";
import { CommandService } from "../../../src/ops/command-service.js";
describe("command service",()=>{it("canonicalizes parameters for deterministic idempotency and audits",async()=>{const commands={create:vi.fn().mockResolvedValue({id:"c1",jobName:"collect_x_posts"})} as any;const audit={record:vi.fn()} as any;const s=new CommandService(commands,audit);await s.enqueue({jobName:"collect_x_posts",parameters:{b:2,a:1},actor:"ops"});await s.enqueue({jobName:"collect_x_posts",parameters:{a:1,b:2},actor:"ops"});expect(commands.create.mock.calls[0][0].idempotencyKey).toBe(commands.create.mock.calls[1][0].idempotencyKey);expect(audit.record).toHaveBeenCalledTimes(2);});it("routes legal transition functions",async()=>{const commands={call:vi.fn().mockResolvedValue({})} as any;const s=new CommandService(commands);await s.claim("w","local",["x.posts.collect"]);await s.start("id","w");await s.bind("id","w","run");await s.complete("id","w","succeeded");await s.cancel("id","ops");expect(commands.call.mock.calls.map((x:any)=>x[0])).toEqual(["claim_job_command","start_job_command","bind_job_run","complete_job_command","cancel_job_command"]);});});
