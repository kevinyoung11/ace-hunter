import { guard, output, fail } from "@/lib/ops/http"; import { opsService } from "@/lib/ops/service"; export const runtime="nodejs"; export async function POST(req:Request){const g=guard(req,true);if(g.response)return g.response;if(req.headers.get("x-ops-tick-secret")!==process.env.ACE_HUNTER_OPS_TICK_SECRET)return fail("tick_forbidden",403,g.id);try{return output(await opsService().tick(),g.id,202)}catch{return fail("tick_failed",502,g.id)}}
